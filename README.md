# Tracker (Memory Version)

This starter includes:
- `client/` React app (Vite)
- `server/` Express API using an in-memory array

## Run the server
cd server
npm install
npm run dev

## Run the client (in a separate terminal)
cd client
npm install
# copy .env.example -> .env if you want to change API base
npm run dev
